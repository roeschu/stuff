#IP auf welche Namen basierte VHOSTS reinkommenNameVirtualHost 192.168.10.10:80#Nur wenn SSL verwendet wirdNameVirtualHost 192.168.10.10:443#Beispiel Vhost Container mit Beispieldirektiven<VirtualHost www.domain.com:80>	#Servername + Hostheaders	ServerName www.domain.com	ServerAlias domain.com	ServerAdmin bla@com.com	#Beispiel Redirect, leitet www.domain.com/test auf www.domain.com um	Redirect /test http://www.domain.com	# An/abschalten SSL	SSLEngine off	#Webroot	DocumentRoot /var/www/domain_com/	#schaltet Rewrite Eninge ein	RewriteEngine On	# Verhindert Requests mit TRACE und TRACK  	RewriteCond %{REQUEST_METHOD} ^(TRACE|TRACK)    	RewriteRule .* - [F]	#Besipiel für einfache RewriteRule	RewriteRule ^/images/cartoon$ http://www.waltdisney.com/cartoon [L,R]	#Standards für Optionen in Directory	<Directory /var/www/domain_com>		#Kein Optionen aussert den beiden mit + vorangestellt        	Options None +FollowSymLinks +MultiViews     			#Uebersteuerung von Direktiven wenn .htaccess		AllowOverride None		#Niemand darf zugreifen...		Order allow, deny		#...aussert diese hier..		allow from all    	</Directory>	#Beispiel für geschütztes Dir, mit AuthConfig können Authorisieriungsanweisungen definiert werden	<Directory "/var/www/domain_com/secure">  			Options None +FollowSymLinks +MultiViews        	AllowOverride AuthConfig        	Order allow,deny        	allow from all    	</Directory>	#Beispiel für keinen Zugriff auf das Verzeichnis	 <Directory "/var/www/domain_com/wichtigefiles">        	deny from all   	 </Directory>	#Definition der cgi Umgebung	ScriptAlias /cgi-bin/ /usr/lib/cgi-bin/    	<Directory "/usr/lib/cgi-bin">        	AllowOverride None        	Options ExecCGI -MultiViews +SymLinksIfOwnerMatch        	Order allow,deny        	Allow from all    	</Directory>    	# Log settings   	LogLevel warn	ErrorLog /var/log/apache2/error.log	CustomLog /var/log/apache2/access.log combined</VirtualHost>    	